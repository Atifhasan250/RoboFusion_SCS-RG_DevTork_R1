import { collections } from "../db/collections";
export async function riskTrend(zoneId:string){const readings=await (await collections()).readings.find({zoneId}).sort({observedAt:-1}).limit(8).toArray();if(readings.length<3)return{status:"INSUFFICIENT_DATA",slope:0,window:readings.length};const ordered=[...readings].reverse(),n=ordered.length,meanX=(n-1)/2,meanY=ordered.reduce((s,r)=>s+r.riskScore,0)/n;let top=0,bottom=0;ordered.forEach((r,i)=>{top+=(i-meanX)*(r.riskScore-meanY);bottom+=(i-meanX)**2});const slope=bottom?top/bottom:0;return{status:slope>=3?"RISING":slope<=-3?"FALLING":"STABLE",slope:Math.round(slope*100)/100,window:n,latestRisk:ordered.at(-1)!.riskScore}}
