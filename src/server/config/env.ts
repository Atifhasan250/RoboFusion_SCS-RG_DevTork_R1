import { z } from "zod";
const schema = z.object({ MONGODB_URI: z.string().default("mongodb://127.0.0.1:27017/?replicaSet=rs0"), MONGODB_DB: z.string().default("robofusion"), SESSION_SECRET: z.string().min(16).default("development-only-change-this-secret"), ZONE_API_KEY_PEPPER: z.string().min(8).default("development-zone-pepper"), APP_ORIGIN: z.string().url().default("http://localhost:3000"), APP_ENV: z.enum(["development", "test", "production"]).default("development"), GEMINI_API_KEY: z.string().optional(), GEMINI_MODEL: z.string().default("gemini-3.5-flash"), OPENROUTER_API_KEY: z.string().optional(), OPENROUTER_PRIMARY_MODEL: z.string().default("google/gemini-3.5-flash"), AI_TIMEOUT_MS: z.coerce.number().int().positive().default(8000) });
export const env = schema.parse(process.env);
